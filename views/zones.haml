%title
  = title
%body
  #zones
    %tbody
      %h4.city-title
        = title
      - zones.each do |zone|
        = haml :render_by_value, :locals => { :pm2_5 => zone[:pm2_5], :position => zone[:position_name] }
      = haml :search_box
      = haml :top_last_button
      = haml :footer_with_adsense, :locals => { :time => time }



